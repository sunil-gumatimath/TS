let drink = 'coffee'

let cups = Math.random() > 0.5 ? 10 : '5'  // inference means // → type **number | string**

let channelName = 'sunilted' // inference `string`

let coffeeFlavour : string = 'Caramel'   // annotations and inference as `string`
 
coffeeFlavour = 'nutty'

let orderCoffee : boolean = true  // annotations

